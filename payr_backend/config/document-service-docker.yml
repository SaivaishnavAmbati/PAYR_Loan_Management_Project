server:
  port: 8180

spring:
  application:
    name: DOCUMENT-SERVICE
  datasource:
    url: jdbc:mysql://mysql:3306/payr_document_db?createDatabaseIfNotExist=true
    username: root
    password: ${DB_PASSWORD:6303}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true


eureka:
  instance:
    prefer-ip-address: true
    hostname: document-service
    instance-id: document-service:${server.port}
  client:
    service-url:
      defaultZone: http://discovery-server:8761/eureka
    register-with-eureka: true
    fetch-registry: true


springdoc:
  swagger-ui:
    path: /swagger-ui.html

management:
  tracing:
    sampling:
      probability: 1.0
    enabled: true

  zipkin:
    tracing:
      endpoint: http://zipkin:9411/api/v2/spans


cloud:
  aws:
    credentials:
      access-key: ${ACESS_KEY:}
      secret-key: ${SECRET_KEY:}
    region:
      static: ap-south-2